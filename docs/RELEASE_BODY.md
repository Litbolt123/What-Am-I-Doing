# What Am I Doing 1.2.4 — Windows installer

## What's new

### Rules
- **Rules editor scrolls** — the bottom pane (capture, match type, pattern, advanced fields) now scrolls inside the window instead of requiring you to resize the Rules dialog to see everything.

## Notes

- Quit the app from the tray before running the installer.
- Updating over **1.2.3** keeps your activity database and settings.
