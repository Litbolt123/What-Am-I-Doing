# What Am I Doing 1.1.3 — Windows installer (updater smoke test)

## What's new

- **Internal / test build:** Use this release to verify **Settings → Updates** (startup check + tray tip) and **About → Download and run installer…** after installing **1.1.2** from the previous GitHub release. No functional changes are intended beyond the version bump.

## Notes

- Install **1.1.2** first from its release asset, then let this build appear as “newer” or fetch it manually to exercise the update flow.
